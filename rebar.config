{erl_opts, []}.
{relx, [{release, {kterl, "1.2.0"},
         [kterl, runtime_tools, observer, wx]},

        {dev_mode, false},
        {include_erts, false},

        {extended_start_script, true}]}.
